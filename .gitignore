# Installed packages must be recreated with npm install.
node_modules/
# Private environment values must never be committed.
.env
# Local logs and coverage output are not source files.
*.log
coverage/
